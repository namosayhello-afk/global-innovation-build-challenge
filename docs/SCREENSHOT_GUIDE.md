# FetalSignal AI — screenshot guide

Capture these three screenshots from the deployed app after testing the upload flow. They satisfy the competition's minimum of three project screenshots and tell a clear product story.

| Screenshot | How to capture it | Suggested Devpost caption |
| --- | --- | --- |
| 1. Input and overview | Upload `sample_data/fetalsignal_sample_recording.csv`, select `abdominal_ecg`, and show the Overview tab with the result cards and two waveform charts. | “FetalSignal AI turns a de-identified abdominal ECG waveform into an inspectable fetal cardiac-signal candidate.” |
| 2. Transparent signal lab | Open **Signal lab** and capture all three separation layers plus the export controls. | “The full separation path is visible: filtered mixture, maternal-pattern estimate, and residual candidate signal.” |
| 3. Validation and evidence | Capture the **Validation** tab after adding the synthetic reference file, then capture the **Evidence** tab if Devpost permits a fourth image. | “Independent references produce precision, recall, F1, and rate-error metrics; public-data evidence is reported record by record.” |

Use full-width desktop screenshots. Do not show personal files, patient identifiers, browser tabs, API keys, or terminal output. The included sample files are synthetic, so they are safe for screenshots but must never be described as the public-dataset evaluation.
