# FetalSignal AI — approximately 3 minute 45 second demo video

Record a screen capture of the deployed app with your voice **or** these English captions. A face camera is optional; the important part is showing the working system and explaining the approach. Do not call a candidate result a diagnosis.

| Time | Show on screen | Say or caption |
| --- | --- | --- |
| 0:00–0:18 | App home and research-only safety statement | “FetalSignal AI is an explainable research dashboard for exploring fetal cardiac-signal candidates inside mixed abdominal ECG recordings. It was designed to make the full research workflow visible instead of giving a black-box answer.” |
| 0:18–0:38 | Open **New here? Follow the guided walkthrough**, click Next step, then collapse it to continue | “Abdominal ECG is difficult because the fetal signal is mixed with a stronger maternal pattern, motion, and electrical noise. The app includes a walkthrough that tells a new user how to choose a recording, understand the results, and check the possible beats.” |
| 0:38–0:58 | Click **Upload an ECG recording**; upload `sample_data/fetalsignal_sample_recording.csv` | “The app accepts a de-identified CSV or TXT waveform with one sample per row. It intentionally does not accept an image of an ECG chart, because original signal values are needed for reliable, repeatable processing.” |
| 0:58–1:16 | Set **B. Sampling rate** to 500 Hz. Open **C. Add matching reference beats**, upload `fetalsignal_sample_reference_beats.csv`, choose **Seconds**, and keep `abdominal_ecg` in lead selection D | “For this demonstration, I’m using the included synthetic sample recording at 500 hertz. I also add a separate reference-beat file. Keeping references separate is important because it lets us evaluate the algorithm instead of simply trusting it.” |
| 1:16–1:38 | Overview tab; point to candidate BPM, quality card, and waveform charts | “The overview shows the mixed abdominal ECG beside the residual fetal cardiac-signal candidate. The result cards show a candidate rate, number of candidate beats, maternal rate, and a signal-quality heuristic. These are research outputs, not a diagnosis or patient reading.” |
| 1:38–1:52 | Move the recording-window slider | “I can move through the recording to inspect a different time window. The markers show algorithmic candidate peaks, so the next step is always to visually review them and compare them with an independent reference.” |
| 1:52–2:15 | Signal lab | “The Signal lab makes the processing transparent. First, it filters the abdominal mixture. Next, it estimates the repeated maternal pattern. Finally, it shows the residual where the fetal candidate peaks are found. The researcher can download the processed waveform, candidate beats, and a short analysis report.” |
| 2:15–2:33 | Validation tab | “With separate reference beats, the Validation tab reports precision, recall, F1 score, and candidate-rate error. That gives us a concrete way to test the pipeline on documented research data rather than making an unsupported claim from a single chart.” |
| 2:33–2:58 | Evidence tab; point to macro metrics and public-record table | “For real public-data evidence, we evaluated the multi-lead consensus approach on five de-identified PhysioNet recordings. It reached a macro F1 of 95.77 percent, with 0.60 BPM candidate-rate error. The table shows the results for every record.” |
| 2:58–3:12 | Point to Evidence limitation warning | “These are exploratory results from a small public dataset, not clinical-performance claims. They do not predict how the method will perform for a new person or a real patient.” |
| 3:12–3:25 | Method tab; point to multi-lead and limitations bullets | “The pipeline filters the signal, estimates and reduces the maternal template, finds residual candidates, and—when three or more aligned abdominal leads are available—keeps candidates supported by several leads. The app also makes its limitations clear.” |
| 3:25–3:45 | Return to the safety statement | “FetalSignal AI uses only public, de-identified or synthetic data. It is a research prototype, not a medical device, and it must never be used for patient decisions.” |

Before recording, run the app once, increase browser zoom only if text becomes too small, and use a clean browser window. Keep the video between 2 and 5 minutes, as required by the competition.
